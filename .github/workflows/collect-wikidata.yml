name: Collect WikiData

on:
  schedule:
    - cron: "0 2 * * 1"
  workflow_dispatch:

concurrency:
  group: conference-aggregator-collectors
  cancel-in-progress: false
  queue: max

permissions:
  contents: write

jobs:
  collect-wikidata:
    runs-on: ubuntu-latest
    name: Collect WikiData

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Update main
        run: |
          git pull --ff-only origin main

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Collect WikiData
        run: pnpm collect:wikidata.org

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add conference-postings.json conference-postings-full.json

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "chore: update Wikidata conference data"
          git push origin main
