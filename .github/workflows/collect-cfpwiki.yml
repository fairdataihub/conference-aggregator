name: collect-cfpwiki

on:
  schedule:
    - cron: "0 2 * * 1"
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  collect-cfpwiki:
    runs-on: ubuntu-latest
    name: Collect cfp.wiki
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Collect cfp.wiki conferences
        run: pnpm exec tsx scripts/conference-collecting/main.ts --site cfpwiki

      - name: Check for changes
        id: git-status
        run: |
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create Pull Request
        if: steps.git-status.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "chore: update conference list (cfp.wiki)"
          title: "chore: update conference list (cfp.wiki)"
          body: |
            Automated conference list update (cfp.wiki).

            Generated at:
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          branch: update/conferences-cfpwiki-${{ github.run_number }}
          base: main
          delete-branch: true

